import pygame
import esper


class LocationProcessor(esper.Processor):
    def process(self, dt, running):
        pass


class PositionProcessor(esper.Processor):
    def process(self, dt, running):
        pass
